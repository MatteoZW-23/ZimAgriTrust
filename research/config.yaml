project: AgriTrust Research
version: 1.0.0
ml_settings:
  train_split: 0.8
  random_state: 42
  contamination_rate: 0.01

paths:
  raw_data: ./data/raw
  processed_data: ./data/processed
  models: ../ml_weights
  reports: ./reports

scheduler:
  interval_hours: 24
  automated_retraining: true
